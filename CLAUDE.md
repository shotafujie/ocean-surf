# OCEAN SURF — 開発メモ

ブラウザで動く 3D サーフィンレース。`index.html` 一枚（ビルド工程なし、外部依存は three.js r128 の CDN のみ）。

## 動かす / 検査する

```bash
npm install          # 検査ツール用 (web-audio-engine)
npm run serve        # http://localhost:8000 で開く
npm test             # 実行時 + レース + 音声 の全検査
```

| コマンド | 何を見るか |
|---|---|
| `npm run test:runtime` | 実際に数分ぶん走らせて例外・NaN・状態破綻・音声ノードのリーク |
| `npm run test:race` | NPC だけで完走させてタイム・順位・技量の単調性 |
| `npm run test:race:detail` | 上記 + 区間タイムと加速度の内訳（斜面/パンプ/抗力/旋回） |
| `npm run test:audio` | 効果音をオフラインレンダリングして音量・周波数・クリッピング |

ブラウザを開けない状況でも、この3つを通せば「起動して遊べる状態か」はほぼ判定できる。**挙動を変えたら必ず `npm test` を通すこと。**

## 構造

```
index.html          ゲーム本体。これ一枚で完結
tools/load.js       index.html の <script> を THREE/DOM スタブ環境で評価して内部関数を返す
tools/race-sim.js   NPC レースのシミュレーション
tools/audio-test.js 効果音の波形・スペクトル検査
tools/runtime-check.js 実行時の総合チェック
```

`index.html` の末尾に、ブラウザでは無効化されるテスト用エクスポートがある：

```js
if (typeof module !== "undefined" && module.exports) { module.exports = { ... }; }
```

**テスト側に物理や AI を書き写さないこと。** 二重管理になると「テストは通るのにゲームは直っていない」が起きる。必要な関数はここに足す。

## 絶対に壊してはいけない不変条件

### 1. 波の式は CPU と GPU で完全に一致していること

`h(x,z) = Σ Aᵢ·sin(kᵢ·dot(Dᵢ,p) − ωᵢ·t)` を、頂点シェーダー（描画）と `waveInfo()`（物理）が**別々に実装している**。`WAVE_DEFS` を変えたら両方が同じ値を返すことを必ず確認する。ズレると「見えている波」と「ボードが乗る波」が食い違い、原因の特定が非常に困難になる。

シェーダー側はユニフォーム `uDir` / `uPar` / `uOmega` に JS 側から流し込んでいるので、係数を足す・減らす場合は `#define NW 4` も合わせる。

### 2. `waveInfo()` は使い回しのオブジェクトを返す

```js
var wi = waveInfo(x, z, t);
// ここで別の waveInfo() を呼ぶと wi の中身が書き換わる
```

必ず即座にスカラーへ退避する（`var wy = wi.h, gx = wi.gx, gz = wi.gz;`）。過去にリング・魚の高さ計算がプレイヤーの勾配を上書きし、板の傾きが暴れるバグを出している。毎フレーム大量に呼ぶのでアロケーションを避けたい意図があり、この設計自体は残す。

### 3. プレイヤーと NPC は同じ `stepSurfer()` を通る

ハンデも補正もない。ゲートの捕捉半径も同一（`g.half`）。ここに例外を持ち込むと「実力勝負」が崩れる。

## 物理

`stepSurfer(o, inp, dt)`。`inp = {turn(-1..1), pump, brake, jump}`。

| 項 | 値 | 備考 |
|---|---|---|
| 斜面加速 | `-(∇h·heading) × 34` | 支配的。斜度0.3で約10 m/s² |
| パンプ推力 | `11 × thrust` | NPC は thrust で個体差 |
| 抗力 | `0.055·v² + 0.9` | パンプのみの平衡は約12.6 m/s |
| 旋回損失 | `|turn| × v × 0.16` | 舵を切りっぱなしだと露骨に失速する |
| 速度上限 | 34 (ブースト時40) | |

**うねりは +Z 方向へ 8.4 m/s で進む。** まっすぐ +Z に突っ切ると波の背を登り続け、斜面項の時間平均が **−5 m/s²** にもなる（`npm run test:race:detail` で計測できる）。速く走るには波の面を斜めに滑り続けるしかない。この性質がゲーム性の中核。

## コース

シード固定（`COURSE_SEED = 20260727`）。うねりに対して **±62° のジグザグ**、7ゲート・全長 1283m。

まっすぐなコースにすると上記の理由で誰も速く走れず、平均 10 m/s 程度で頭打ちになる。斜めに刻むレイアウトにすると 16 m/s まで伸びる。**コース形状を変えるときはこの制約を忘れないこと。**

ゲート判定は平面通過ではなく**中心からの半径**。ジグザグだと平面の向きが交互に反転し、次ゲートの平面をすでに越えた状態でスタートしてしまって永久に通過できなくなるため。

## NPC AI

`npcInput(o, dt)`。候補ヨーを ±78°／15°刻みで 11 方向試し、次の値が最大の向きを選ぶ：

```
value = min(19, max(0.5, speed + downhill × 34 × T)) × cos(候補ヨー − ゲート方向)
```

**この形にした理由（ここを崩すと必ず壊れる）:**

- 斜面の加速を「T 秒後の速度」として織り込んだうえで、**ゲート方向成分に cos で射影する**。加速度を直接評価する形にすると、上手い AI ほど波に釣られてゲートを無視し延々サーフィンして DNF する。
- `min(19, ...)` の頭打ちが無いと、急斜面の候補が他をすべて圧倒して同じことが起きる。
- レーシングラインの先読みは**狙点をゲート内に収める**こと（`±half × 0.7` にクランプ）。過去に狙点を次ゲート側へ最大60%ずらしていて、捕捉半径 13m に永久に入れず全 DNF した。
- 目標方位は平滑化する（`aim` を 3.0/s で追従）。波の位相で毎フレーム暴れる的を直接追うと舵が振動して失速する。

技量パラメータ（`NPC_DEF`）：

| | skill | thrust | 期待タイム |
|---|---|---|---|
| KAI | 0.30 | 0.80 | ≒108s |
| NOA | 0.55 | 0.90 | ≒103s |
| RIO | 0.80 | 0.98 | ≒98s |
| ZEN | 1.00 | 1.06 | ≒89s |

`npm run test:race` はこのタイムと、**技量が高いほど速いという単調性**を検査する。単調性が崩れたら AI の目的関数が壊れているサイン。

## サウンド

音源ファイルなし。Web Audio で全合成（`AU` モジュール）。最初のキー入力で `AU.init()`。`M` でミュート。

砕波音は演出ではなく、毎フレーム周囲 22〜80m の海面を実際にサンプリングして、波高2.5m超かつ斜度0.52超の地点を探し、その方向にパンして鳴らしている。

**Web Audio の落とし穴（実際に踏んだもの）:**

- `linearRampToValueAtTime` のあとに `setValueAtTime` を置くと、以降が無音になる実装がある。保持区間は**同値への linearRamp** で作る。これで笛が50msで切れていた。
- ローパスを下降スイープさせるとノイズのエネルギーが約 −16dB 落ちる。`SPLASH_MAKEUP` で補償している。
- マスターのフェードインを長くすると（時定数0.8s）、直後に鳴るカウントダウンの最初の笛が飲まれる。現在 0.06s。
- 最終段に等倍ソフトクリップ（|x|<0.7 は素通し）。全域 tanh にすると小信号にもゲインがかかって全体が持ち上がる。

## three.js r128 の制約

CDN 固定で r128。`CapsuleGeometry` は r142 以降なので**使えない**。`OrbitControls` 等のアドオンも読み込んでいない。`outputEncoding` / `sRGBEncoding`（新 API の `colorSpace` ではない）。

## その他の注意

- レーサー定義に `thrust` が無いと `11 × undefined` で速度が NaN になる。`makeRacer` で既定値 1.0 を入れているが、新しいレーサーを足すときは意識する。
- 描画は `index.html` 内の ShaderMaterial 直書き。GLSL は JS の文字列配列なので、構文エラーはブラウザのコンソールにしか出ない（`npm test` では検出できない）。**シェーダーを触ったら必ずブラウザで開いて確認すること。**
- ベストタイムは `localStorage` の `oceansurf_best_v1`。`file://` だとブラウザによっては効かないので、確認は `npm run serve` 経由で。

## 公開

GitHub Pages にそのまま置ける。`index.html` がトップページになる。詳細は README.md。
